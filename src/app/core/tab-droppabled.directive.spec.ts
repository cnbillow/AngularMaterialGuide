import { TabDroppabledDirective } from './tab-droppabled.directive';

describe('TabDroppabledDirective', () => {
  it('should create an instance', () => {
    const directive = new TabDroppabledDirective();
    expect(directive).toBeTruthy();
  });
});
