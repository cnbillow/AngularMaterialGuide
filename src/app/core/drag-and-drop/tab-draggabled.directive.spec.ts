import { TabDraggabledDirective } from './tab-draggabled.directive';

describe('TabDraggabledDirective', () => {
  it('should create an instance', () => {
    const directive = new TabDraggabledDirective();
    expect(directive).toBeTruthy();
  });
});
